/** Shared chart palette, driven by CSS variables so it adapts to theme. */
export const CHART_COLORS = [
  "hsl(var(--chart-1))",
  "hsl(var(--chart-2))",
  "hsl(var(--chart-3))",
  "hsl(var(--chart-4))",
  "hsl(var(--chart-5))",
];

export const AXIS_STYLE = {
  fontSize: 11,
  fill: "hsl(var(--muted-foreground))",
};

export const GRID_STROKE = "hsl(var(--border))";
